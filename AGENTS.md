# AGENTS.md: devdongin.github.io

이 저장소는 선동인의 개인 포트폴리오 사이트다 (GitHub Pages, 단일 `index.html`).

## 에이전트 역할 분담

- **Codex가 디자인 디렉션과 리뷰를 담당한다.** 실제 렌더링을 기준으로 UI/UX, 시각적 위계, 반응형, 접근성, 이미지 생성·선정 방향을 제안하고 구현 방식의 보안·개인정보·성능 위험을 검토한다.
- **Claude Code가 구현을 담당한다.** Codex의 최신 인계 문서와 Issue를 바탕으로 콘텐츠, HTML/CSS/JS, 에셋, 검증, PR 준비를 수행한다.
- **Codex는 기본적으로 design-and-review-first다.**
  - 일반적인 콘텐츠·디자인·구현 변경에는 코드를 직접 수정하지 않고 리뷰 코멘트나 GitHub Issue를 남긴다.
  - 다만 정보 구조 개편, 핵심 메시지 변경, 주요 프로젝트 추가, 사이트 전반의 접근성·반응형 개선처럼 영향 범위가 큰 변경은 Codex도 직접 구현할 수 있다.
  - 큰 변경을 시작하기 전 GitHub Issue나 저장소에 남는 인계 문서로 Claude에게 변경 목적·범위·검증 기준을 먼저 알린다.
  - 직접 구현할 때는 별도 브랜치와 PR을 사용하고, PR 본문에 Claude가 이어서 확인할 수 있는 변경 요약과 검증 결과를 남긴다. `main` 직접 push는 하지 않는다.
  - 사용자가 Codex에게 특정 변경을 명시적으로 요청한 경우에는 위 큰 변경 예외와 같은 절차로 수행한다.

## Codex 리뷰 관점 (우선순위순)

1. **민감 정보 유출**: 전화번호, 집주소, 연봉, 출생연도/나이, 학점이 발견되면 최우선(critical)으로 보고. 이메일은 Contact 섹션 외 위치에 있으면 보고.
2. **사실관계**: 이름은 반드시 "선동인" (손동인은 오기). 경력 기간·수치·프로젝트명이 기존 내용과 모순되면 보고.
3. **HTML/CSS 일관성**: CSS 변수 대신 리터럴 색상 하드코딩, 기존 컴포넌트(.skill-card/.project/.badge/.chip) 미재사용, `.reveal` 클래스 누락, 섹션 배경 교차(기본↔.alt) 붕괴.
4. **접근성/반응형**: alt 텍스트 누락, 720px 이하 레이아웃 깨짐, 가로 오버플로.
5. **디자인 스킬 기준(2026-08-01 도입, CLAUDE.md 참조)**: 이모지 사용, keep-all 누락, 네온 글로우, CTA 상태(hover/active/focus) 누락, 인접 섹션 레이아웃 반복, AI 클리셰 카피를 발견하면 보고. 기준 원문: anthropics/skills frontend-design · uxjoseph/supanova-design-skill · nextlevelbuilder/ui-ux-pro-max-skill.
6. **엠 대시 사용(2026-08-04 사용자 지시)**: 본문·문서·커밋 메시지에 `—`가 있으면 보고한다. AI가 쓴 글의 표시로 읽히기 때문이다. 엔 대시 `–`는 날짜 범위(`2022.07 – 현재`)에만 허용. 대체 표기는 CLAUDE.md 절대 규칙 6번 참조.

리뷰 결과는 파일:줄 위치와 함께 간결한 목록으로 제시한다.

## AI REVIEW 작성 기준

- 히어로의 `AI REVIEW`는 **본문 240자·3문장·인용 수치 2개 이내**로 작성한다. `verify_auto_update.py`가 강제한다 (#138 P1).
- 핵심 평가 1문장과 검증 가능한 근거 2문장을 연결하며, 한 문장 요약으로 축소하지 않는다.
- 히어로 태그라인에 이미 있는 서술은 반복하지 않는다. 길어지면 CTA가 첫 화면 밖으로 밀린다.
- 성능·기여 통계·시스템 통합 범위처럼 공개 검증 가능한 사실만 사용하고, 내부 저장소명이나 민감정보는 쓰지 않는다.
- 이 사이트는 제품 홍보 페이지가 아니다. 제품 기능·매출·고객사보다 선동인의 문제 정의, 기술 선택, 구현 범위, 검증 결과가 문장의 주어가 되어야 한다. 단, 매출 1줄 압축과 수행 고객사 5개사+POC 합산 칩은 **사용자가 승인한 증거 인용**이므로 삭제 제안 대상이 아니다 (2026-08-01 하이브리드 결정).

## 이슈 기반 협업 흐름

- Codex는 리뷰 발견 사항을 **GitHub Issue로 등록**할 수 있다 (제목에 `[분류]` 프리픽스).
- **Issue나 PR을 게시하기 직전 반드시 작성 내용을 한 번 최종 리뷰한다.** Issue는 최신 코드 기준의 사실관계, 중복 여부, 영향 범위, 재현 근거, 링크를 확인한다. PR은 제목·본문·실제 diff·검증 결과·관련 Issue와 종료 키워드가 서로 일치하는지 확인한다.
- 게시 후에는 GitHub에서 Issue/PR을 다시 열어 본문과 링크가 의도대로 렌더링됐는지 확인하고, 게시 전 검토에서 놓친 오류가 발견되면 즉시 수정하고 정정 사실을 남긴다.
- Claude가 각 이슈를 점검·처리한 뒤, **코멘트로 처리 결과(반영 커밋, 기반영 확인, 보류 사유)를 남기고 이슈를 종료**한다.
- Codex는 다음 리뷰에서 종료된 이슈의 반영 결과를 검증하고, 미흡하면 새 이슈로 재등록한다.
